query userRepository($login: String!) {
  user(login: $login) {
    name
    url
    repositories(first: 20) {
      totalCount
      nodes {
        name
        description
        createdAt
        updatedAt
        url
      }
    }
  }
}
